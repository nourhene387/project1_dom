
/*document.addEventListener('DOMContentLoaded', () => {
    var el = document.getElementById('quantity');
    var quantityValue = parseInt(el.innerHTML,10);
   
 
    function clickinc() {
        quantityValue++;
        el.innerHTML = quantityValue; // Met à jour l'élément avec la nouvelle valeur
        console.log(quantityValue); // Affiche la nouvelle valeur dans la console
    }

    // Ajoute un gestionnaire d'événements au bouton
    document.getElementById('incrementBtn').addEventListener('click', clickinc);
});*/



